The administrator has the permission to view each order, and each user's order data is kept separate.

admin:
username: admin
password: admin

bundle install
rails server

rails db:migrate
rails db:seed
